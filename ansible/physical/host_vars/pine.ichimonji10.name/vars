---
btrfs_paths:
  - /mnt/btrfs

btrfs_subvolumes:
  - path: '{{ btrfs_paths[0] }}/srv-media'
  - path: '{{ btrfs_paths[0] }}/-srv-media'
    snapshot: false
    delete: false

dynamic_dns_urls: '{{ vault_dynamic_dns_urls }}'

netctl_generic_external_if: enp2s0

syncthing_usernames:
  - ichimonji10

# vim:ft=yaml:
